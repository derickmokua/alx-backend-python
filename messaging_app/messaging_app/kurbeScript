 #!/bin/bash

# kurbeScript
# Script to set up and verify a Kubernetes cluster locally using Minikube

# Exit immediately if a command fails
set -e

echo "=== Checking if Minikube is installed... ==="
if ! command -v minikube &> /dev/null
then
    echo "Minikube not found. Please install Minikube before running this script."
    echo "Installation guide: https://minikube.sigs.k8s.io/docs/start/"
    exit 1
else
    echo "Minikube is installed ✅"
fi

echo "=== Starting Minikube cluster... ==="
minikube start

echo "=== Verifying Kubernetes cluster info... ==="
kubectl cluster-info

echo "=== Listing all available pods in the default namespace... ==="
kubectl get pods

echo "=== Kubernetes cluster is up and running 🚀 ==="
